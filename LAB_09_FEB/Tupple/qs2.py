# Create a tuple containing different data types (int, float, string, boolean).
# Print each element with its data type.

values = (5,6,"Ankur","Rik", True)

for i in values:
    print(f"value:{i} and it's {type(i)} of data")